# (C) Copyright 2025 WeatherGenerator contributors.
#
# This software is licensed under the terms of the Apache Licence Version 2.0
# which can be obtained at http://www.apache.org/licenses/LICENSE-2.0.
#
# In applying this licence, ECMWF does not waive the privileges and immunities
# granted to it by virtue of its status as an intergovernmental organisation
# nor does it submit to any jurisdiction.
#
# Forecasting fine-tuning starting from a MAE pre-trained checkpoint.
#
# Frozen:   encoder (ae_local, ae_global, ae_aggregation, embed_engine)
#           latent_pre_norm, latent_heads  (SSL-only components, unused here)
# Trainable: forecast_engine (fe_blocks), pred_heads (decoder)
#
# Frozen module regex: ".*encoder.*|.*latent_pre_norm.*|.*latent_heads.*"

# number of steps offset applied to first target window; if set to zero and forecast_steps=0 then
# one is training an auto-encoder
fe_num_blocks: 6
fe_num_heads: 16
fe_dropout_rate: 0.1
fe_with_qk_lnorm: True
fe_layer_norm_after_blocks: []  # Index starts at 0. Thus, [3] adds a LayerNorm after the fourth layer
fe_impute_latent_noise_std: 0.0
# currently fixed to 1.0 (due to limitations with flex_attention and triton)
forecast_att_dense_rate: 1.0
with_step_conditioning: True

healpix_level: 5

# performance
with_mixed_precision: True
with_flash_attention: True
compile_model: False
with_fsdp: False
ddp_find_unused_parameters: False
attention_dtype: bf16
mixed_precision_dtype: bf16
mlp_norm_eps: 1e-5
norm_eps: 1e-4

latent_noise_kl_weight: 0.0
latent_noise_gamma: 2.0
latent_noise_saturate_encodings: 5
latent_noise_use_additive_noise: False
latent_noise_deterministic_latents: True

# Freeze the whole encoder; leave forecast_engine (fe_blocks) and pred_heads (decoder) trainable.
# - .*encoder.*       → EncoderModule and all sub-engines (ae_local, ae_global, ae_aggregation, embed)
# - .*latent_pre_norm.* → latent normalisation layer (SSL-only, not used in forecasting)
# - .*latent_heads.*  → latent prediction heads (SSL-only, not used in forecasting)
freeze_modules: ".*encoder.*|.*latent_pre_norm.*|.*latent_heads.*"

norm_type: "LayerNorm"

# type of zarr_store
zarr_store: "zip" # "zarr" for LocalStore, "zip" for ZipStore

#####################################

streams_directory: "./config/streams/era5_1deg/"
streams: ???

general:

  # mutable parameters
  istep: 0
  rank: ???
  world_size: ???

  # local_rank,
  # with_ddp,
  # data_path_*,
  # model_path,
  # run_path,
  # path_shared_

  multiprocessing_method: "fork"

  desc: ""
  run_id: ???
  run_history: []

# logging frequency in the training loop (in number of batches)
train_logging:
  terminal: 10
  metrics: 20
  checkpoint: 250
  log_grad_norms: False

# parameters for data loading
data_loading:

  num_workers: 12
  rng_seed: ???
  repeat_data_in_mini_epoch: False

  memory_pinning: True


# config for training
training_config:

  # training_mode: "masking", "student_teacher", "latent_loss"
  training_mode: ["masking"]

  num_mini_epochs: 32
  samples_per_mini_epoch: 4096
  shuffle: True

  start_date: 1979-01-01T00:00
  end_date: 2022-12-31T00:00

  time_window_step: 06:00:00
  time_window_len: 06:00:00

  learning_rate_scheduling:
    lr_start: 1e-6
    lr_max: 5e-5
    lr_final_decay: 1e-6
    lr_final: 0.0
    num_steps_warmup: 512
    num_steps_cooldown: 512
    policy_warmup: "cosine"
    policy_decay: "constant"
    policy_cooldown: "linear"
    parallel_scaling_policy: "sqrt"

  optimizer:
    grad_clip: 1.0
    weight_decay: 0.1
    log_grad_norms: False
    adamw:
      # parameters are scaled by number of DDP workers
      beta1: 0.975
      beta2: 0.9875
      eps: 2e-08

  losses: {
    "physical": {
        type: LossPhysical,
        loss_fcts: { "mse": { }, },
        },
    }

  model_input: {
    "forecasting": {
      # masking strategy: "forecast" passes all tokens unmasked as input to the encoder
      masking_strategy: "forecast",
      },
    }

  forecast:
    time_step: 06:00:00
    num_steps: 2
    offset: 1
    policy: "fixed"


# validation config; full validation config is merge of training and validation config
validation_config:

  samples_per_mini_epoch: 256
  shuffle: False

  start_date: 2023-10-01T00:00
  end_date: 2023-12-31T00:00

  # whether to track the exponential moving average of weights for validation
  validate_with_ema:
    enabled: False
    ema_ramp_up_ratio: 0.09
    ema_halflife_in_thousands: 1e-3

  # parameters for validation samples that are written to disk
  output: {
    # number of samples that are written
    num_samples: 8,
    # write samples in normalized model space
    normalized_samples: False,
    # output streams to write; default all
    streams: null,
  }

  # run validation before training starts (mainly for model development)
  validate_before_training: False


# Tags for experiment tracking
wgtags:
  org: null
  issue: null
  exp: null
  grid: null
