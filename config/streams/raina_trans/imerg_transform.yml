# (C) Copyright 2025 WeatherGenerator contributors.
#
# This software is licensed under the terms of the Apache Licence Version 2.0
# which can be obtained at http://www.apache.org/licenses/LICENSE-2.0.
#
# In applying this licence, ECMWF does not waive the privileges and immunities
# granted to it by virtue of its status as an intergovernmental organisation
# nor does it submit to any jurisdiction.

IMERG_ANEMOI:
  type: anemoi_transform
  filenames: ["imerg-nasa-grib-o96-1998-2024-6h-v1.zarr"]
  stream_id: 1

  source: ["tp"]
  source_exclude: []
  target: ["tp"]
  target_exclude: []

  target_channel_weights:
    - 1.0
  location_weight : cosine_latitude

  transform_type: log10
  transform_scale: 1000.0    # m -> mm
  transform_offset: 1.0      # log10(x_mm + 1): 0→0, 1mm→0.30, 10mm→1.04, 50mm→1.71

  # transform_type: none

  loss_weight: 1.0
  # Stream-specific loss functions — override the global loss_fcts for IMERG only.
  # ERA5 continues to use global loss_fcts (mse only).

  # Option 1: mse_ens loss with 1.0 weight
  # loss_fcts:
  #   "mse_ens":
  #     weight: 1.0

  # Option 2: kernel_crps with fair to false with 1.0 weight
  # loss_fcts:
  #   "kernel_crps":
  #     weight: 1.0
  #     args:
  #       fair: false

  # Option 3: kernel_crps with fair to false with 0.5 weight and mse loss for 0.5 weight
  # loss_fcts:
  #   "kernel_crps":
  #     weight: 0.5
  #     args:
  #       fair: false
  #   "mse":
  #     weight: 0.5

  # Option 4: kernel_crps with fair to true with 0.5 weight and mse loss for 0.5 weight
  # loss_fcts:
  #   "kernel_crps":
  #     weight: 0.5
  #     args:
  #       fair: true
  #   "mse":
  #     weight: 0.5

  # Original examples:
  # loss_fcts:
  #   "mse":
  #     weight: 0.5
  #   "asymmetric_extreme":
  #     weight: 0.3
  #     args:
  #       floor: 0.1
  #       under_weight: 2.0
  #       over_weight: 0.5
  #   "threshold_exceedance":
  #     weight: 0.2
  #     args:
  #       threshold_quantile: 0.95
  # loss_fcts:
  #   "kernel_crps":
  #     weight: 1.0

  masking_rate: 0.6
  masking_rate_none: 0.05
  token_size: 16
  tokenize_spacetime: True
  max_num_targets: -1

  embed:
    net: transformer
    num_tokens: 1
    num_heads: 8
    dim_embed: 512
    num_blocks: 2
  embed_target_coords:
    net: linear
    dim_embed: 512
  target_readout:
    type: "obs_value"
    num_layers: 2
    num_heads: 4
  pred_head:
    ens_size: 1
    num_layers: 1