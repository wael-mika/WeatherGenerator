# (C) Copyright 2025 WeatherGenerator contributors.
#
# This software is licensed under the terms of the Apache Licence Version 2.0
# which can be obtained at http://www.apache.org/licenses/LICENSE-2.0.
#
# In applying this licence, ECMWF does not waive the privileges and immunities
# granted to it by virtue of its status as an intergovernmental organisation
# nor does it submit to any jurisdiction.
#
# CERRA (~5.5 km / 3h, Europe) as the high-resolution DIAGNOSTIC target -- GENERATIVE arm
# (decoder_type: FlowMatching). Same data, channels and budget as cerra_mse_noup so the arm
# stays comparable; the deltas are all about turning a regression into a sampler.
#
# DELTAS vs cerra_mse_noup:
#   * pred_head.ens_size 1 -> 8. For this decoder ens_size is the number of ODE integrations
#     drawn at evaluation, NOT a set of independent heads (the decoder owns its own velocity
#     readout and pred_head is bypassed entirely). 8 members is enough for the member-SF vs
#     mean-SF coherence read; raise it for a production ensemble.
#   * NO decode_soft_blend_k. Blending averages independent samples inside the transition band,
#     which re-smooths exactly the fine structure the generator exists to produce. The build
#     asserts this. Seams must be judged on single members here, not on a blended field.
#   * 10wdir is kept in the target set (shape parity with the other arms) but is circular, so
#     read its numbers as indicative only.
#
# The loss stays plain 'mse': with the decoder returning y0 + v_theta, mse IS the conditional
# flow-matching objective ||u - v||^2. Do not swap it for a bespoke loss.

CERRA:
  type: anemoi
  filenames: ["cerra-rr-an-oper-se-al-ec-mars-5p5km-1985-2023-3h-v2.zarr"]
  stream_id: 5

  source: []
  source_exclude: []
  target: ['10si', '10wdir', 'tp', '2t']
  target_exclude: ['tp_tp', 'sf_sf', 'ssrd_ssrd', 'strd_strd', 'mn2t', 'mx2t']

  geoinfo_channels: ['lsm', 'orog']

  diagnostic: True
  loss_weight: 1.0

  frequency: "03:00:00"

  masking_rate: 0.6
  masking_rate_none: 0.05
  token_size: 2048
  tokenize_spacetime: True
  max_num_targets: 50000    # target-point subsample; raise toward -1 for production

  embed:
    net: transformer
    num_tokens: 1
    num_heads: 8
    dim_embed: 512
    num_blocks: 2
  embed_target_coords:
    net: linear
    dim_embed: 512
  target_readout:
    type: 'obs_value'
    num_layers: 4
    num_heads: 8
  pred_head:
    ens_size: 8             # = number of ODE samples drawn at evaluation
    num_layers: 2           # unused by FlowMatching (it has its own velocity head)

  # mse against y0 + v_theta == the conditional flow-matching loss (see the decoder docstring).
  loss_fcts:
    "mse":
      weight: 1.0
